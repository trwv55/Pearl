# Анализ соответствия проекта архитектуре Feature-Sliced Design (FSD)

## Общая оценка: ⚠️ Частичное соответствие

Проект использует базовую структуру FSD, но имеет несколько существенных отклонений от стандарта. Для небольшого проекта с одним разработчиком текущая структура функциональна, но требует рефакторинга для полного соответствия FSD.

---

## ✅ Положительные моменты

### 1. **Наличие основных слоев FSD**

- ✅ `entities/` - бизнес-сущности (task, user, stats)
- ✅ `features/` - функциональные возможности (auth, dashboard, TaskForm)
- ✅ `widgets/` - составные компоненты (mainPage, WeeklyStats)
- ✅ `shared/` - переиспользуемые компоненты и утилиты

### 2. **Правильная организация слайсов**

- Слайсы разделены по функциональности (auth, task, dashboard)
- Внутри слайсов есть подразделение (login, register в auth)

### 3. **Использование алиасов импортов**

- Корректная настройка `@/*` в tsconfig.json
- Удобные абсолютные импорты

### 4. **Разделение ответственности в entities**

- Правильное разделение на `api.ts`, `store.ts`, `types.ts` в `entities/task`

---

## ❌ Критические проблемы

### 1. **Папка `components/` - нарушение архитектуры FSD**

**Проблема:** Существует отдельная папка `components/`, которая не является частью FSD архитектуры.

**Текущая структура:**

```
src/components/
  ├── auth/
  ├── dashboard/
  └── taskForm/
```

**Нарушения:**

- Features импортируют из `@/components` вместо использования собственных компонентов
- Компоненты из `components/` должны быть перемещены в соответствующие слои

**Примеры нарушений:**

```typescript
// src/features/auth/login/index.tsx
import { LoginEmail } from "@/components/auth/login/LoginEmail";
import { LoginPassword } from "@/components/auth/login/LoginPassword";

// src/features/TaskForm/index.tsx
import StepCalendar from "@/components/taskForm/StepCalendar";
import { StepCount } from "@/components/taskForm/StepCount";
```

**Рекомендация:**

- Компоненты из `components/auth/` → переместить в `features/auth/`
- Компоненты из `components/dashboard/` → переместить в `features/dashboard/` или `widgets/`
- Компоненты из `components/taskForm/` → переместить в `features/TaskForm/`

### 2. **Папка `lib/` вне структуры FSD**

**Проблема:** Папка `lib/` находится на уровне `src/`, а не внутри слоев.

**Текущая структура:**

```
src/lib/
  ├── auth/
  │   ├── login.ts
  │   ├── logout.ts
  │   └── register.ts
  ├── firebase.ts
  └── utils.ts
```

**Рекомендация:**

- `lib/auth/` → `shared/lib/auth/` (если используется в разных слоях)
- `lib/firebase.ts` → `shared/lib/firebase.ts`
- `lib/utils.ts` → `shared/lib/utils.ts`

### 3. **Папка `providers/` вне структуры FSD**

**Проблема:** Провайдеры находятся вне слоя `app/`.

**Текущая структура:**

```
src/providers/
  ├── AuthProvider.tsx
  ├── ProtectedRoute.tsx
  └── SplashProvider.tsx
```

**Рекомендация:**

- Переместить в `app/providers/` (так как это инициализация приложения)

### 4. **Папка `layouts/` вне структуры FSD**

**Проблема:** Лейауты находятся вне слоев.

**Текущая структура:**

```
src/layouts/
  └── MainPageLayout/
```

**Рекомендация:**

- Переместить в `app/layouts/` или `shared/layout/` (в зависимости от переиспользования)

### 5. **Папка `types/` вне структуры FSD**

**Проблема:** Глобальные типы находятся вне слоев.

**Текущая структура:**

```
src/types/
  └── svg.d.ts
```

**Рекомендация:**

- Переместить в `shared/types/`

---

## ⚠️ Средние проблемы

### 1. **Нарушение правил импортов между слоями**

**Проблема:** Features импортируют из `components/`, что нарушает правило "импорты только вниз".

**Правило FSD:** Слои могут импортировать только из нижележащих слоев:

- `app` → `processes` → `pages` → `widgets` → `features` → `entities` → `shared`

**Текущие нарушения:**

- `features/auth/` → `components/auth/` ❌
- `features/TaskForm/` → `components/taskForm/` ❌
- `widgets/` → `components/dashboard/` ❌

### 2. **Неполная структура сегментов в слайсах**

**Проблема:** Не все слайсы имеют правильную структуру сегментов FSD.

**Рекомендуемая структура слайса:**

```
feature/
  ├── ui/          # UI компоненты
  ├── api/         # API запросы
  ├── model/       # Бизнес-логика, стейт
  ├── lib/         # Утилиты слайса
  └── index.ts     # Публичный API
```

**Текущее состояние:**

- `features/auth/` - частично правильная структура (есть `lib/`, `layout/`)
- `features/dashboard/` - отсутствует структура сегментов
- `entities/task/` - правильная структура (api, store, types)

### 3. **Смешение ответственности в features**

**Проблема:** В `features/auth/index.tsx` есть UI компонент, который должен быть в `ui/` сегменте.

**Рекомендация:**

```
features/auth/
  ├── ui/
  │   └── Auth.tsx        # UI компонент
  ├── model/              # Логика авторизации
  └── index.ts            # Экспорт публичного API
```

---

## 📋 Рекомендации по рефакторингу

### Приоритет 1 (Критично)

1. **Удалить папку `components/`**

   - Переместить компоненты в соответствующие слои
   - Обновить все импорты

2. **Реорганизовать `lib/`, `providers/`, `layouts/`, `types/`**
   - Переместить в соответствующие слои FSD

### Приоритет 2 (Важно)

3. **Исправить структуру слайсов**

   - Добавить сегменты `ui/`, `model/`, `api/` где необходимо
   - Создать публичные API через `index.ts`

4. **Проверить правила импортов**
   - Убедиться, что импорты идут только вниз по слоям
   - Убрать циклические зависимости

### Приоритет 3 (Желательно)

5. **Добавить слой `app/` для инициализации**

   - Переместить провайдеры в `app/providers/`
   - Переместить глобальные настройки в `app/`

6. **Документировать структуру**
   - Создать README с описанием архитектуры
   - Описать правила работы со слоями

---

## 🎯 Упрощенный подход для небольшого проекта

Учитывая, что проект небольшой и с одним разработчиком, можно использовать **упрощенную версию FSD**:

### Минимальные изменения:

1. **Оставить текущую структуру, но:**

   - Переименовать `components/` → `features/` (переместить компоненты в соответствующие features)
   - Переместить `lib/` → `shared/lib/`
   - Переместить `providers/` → `app/providers/`

2. **Упростить структуру слайсов:**

   - Не обязательно создавать все сегменты (`ui/`, `model/`, `api/`)
   - Достаточно разделения на слайсы внутри слоев

3. **Соблюдать правила импортов:**
   - Импорты только вниз по слоям
   - Не импортировать из `components/`

---

## 📊 Текущая структура vs Рекомендуемая FSD

### Текущая:

```
src/
  ├── app/              ✅ (Next.js App Router)
  ├── components/       ❌ (нарушение FSD)
  ├── entities/         ✅
  ├── features/         ✅
  ├── widgets/          ✅
  ├── shared/           ✅
  ├── lib/              ❌ (должно быть в shared/)
  ├── providers/        ❌ (должно быть в app/)
  ├── layouts/          ❌ (должно быть в app/ или shared/)
  └── types/            ❌ (должно быть в shared/)
```

### Рекомендуемая:

```
src/
  ├── app/              ✅
  │   ├── providers/    ✅
  │   └── layouts/      ✅ (если специфичны для app)
  ├── entities/         ✅
  ├── features/         ✅
  │   └── auth/
  │       ├── ui/       ✅
  │       ├── model/    ✅
  │       └── lib/      ✅
  ├── widgets/          ✅
  ├── shared/          ✅
  │   ├── lib/          ✅
  │   ├── types/        ✅
  │   └── layout/       ✅ (если переиспользуются)
  └── pages/            ⚠️ (опционально, если нужен отдельный слой)
```

---

## ✅ Заключение

Проект имеет **хорошую основу** для FSD архитектуры, но требует рефакторинга для полного соответствия. Основные проблемы:

1. Папка `components/` нарушает архитектуру
2. Вспомогательные папки (`lib/`, `providers/`, `layouts/`, `types/`) находятся вне структуры FSD
3. Нарушаются правила импортов между слоями

**Для небольшого проекта** можно использовать упрощенный подход, но базовые принципы FSD (слои, правила импортов) должны соблюдаться.
